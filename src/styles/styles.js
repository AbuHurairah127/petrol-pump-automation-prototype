import {StyleSheet} from 'react-native';

export const commonStyles = StyleSheet.create({
  button: {
    borderRadius: 100,
    backgroundColor: 'red',
    paddingVertical: 8,
    paddingHorizontal: 18,
  },
  buttonLabel: {
    fontSize: 18,
    textAlign: 'center',
    color: 'white',
    letterSpacing: 2,
  },
});
